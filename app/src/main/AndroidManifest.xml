<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cviac.calendar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/panna"
        android:theme="@style/AppTheme">
        <uses-permission android:name="android.permission.INTERNET"/>
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Assert_data" />
        <activity
            android:name=".Daily_calendar"
            android:label="@string/title_activity_tamil_calendar" />
        <activity android:name=".MyCalendarActivity" />

        <activity android:name=".Calendar_event" />
        <activity android:name=".Gridview_tamil" />
        <activity
            android:name=".Registration"
            android:label="Registeration" />
        <activity android:name=".Horoscope_Registeretion" />
        <activity android:name=".Cardview" />
        <activity
            android:name=".Cardview_tab"
            android:label="@string/title_activity_cardview_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Event"
            android:label="Event"
            ></activity>
    </application>

</manifest>