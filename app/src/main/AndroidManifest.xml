<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cviac.calendar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"

        android:theme="@style/AppTheme">
        <activity
            android:name="com.cviac.calendar.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.cviac.calendar.Assert_data" />
        <activity
            android:name="com.cviac.calendar.Tamil_calendar"
            android:label="@string/title_activity_tamil_calendar"
            ></activity>
    </application>

</manifest>